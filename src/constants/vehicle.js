const VEHICLE_TYPES = Object.freeze(["SUV", "Luxury Car", "Mini Bus", "Pickup Vehicle"]);

const VEHICLE_STATUS = Object.freeze({
  AVAILABLE: "Available",
  BOOKED: "Booked",
  MAINTENANCE: "Maintenance",
});

module.exports = {
  VEHICLE_TYPES,
  VEHICLE_STATUS,
};
